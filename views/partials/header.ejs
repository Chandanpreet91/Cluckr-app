<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css" type="text/css">
    <script src="/js/jquery-3.5.1.slim.min.js" type="text/javascript"></script>
    <script src="/js/bootstrap.bundle.min.js" type="text/javascript"></script>
    <title>Cluckr App</title>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="#">Navbar</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="/clucks">cluck! <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="clucks/new">Cluck Master <span class="sr-only">(current)</span></a>
                    </li>
                <% if(signedInUser) { %>
                    <li class="nav-item" >
                        <a class="nav-link" style="color:grey" href="#"> <%= signedInUser%></a>
                    </li>
                    <li class="nav-item">
                        <form action="sign_out" method="POST" class="inline-form">
                            <button class="btn-btn-outline-dark">Sign Out</button>
                        </form>
                    </li>
                  <% }else { %>
                <li class="nav-item">
                    <form action="/sign_in" method="POST" class="form-inline" >
                        <input type="text" name="username" placeholder="Enter your name.." class="form-control"/>
                        <button type="submit" class="btn btn-outline-dark" >
                         Sign In
                        </button>
                    </form>
                 </li>
            <% } %>
                </ul> 
            </div>  
          
        </nav>
 </header>
    
